spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.152.135/gulimail_sms?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: mail-coupon
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
    loadbalancer:
      cache:
        enabled: false

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  #设置主键自增
  global-config:
    db-config:
      id-type: auto

server:
  port: 7000


